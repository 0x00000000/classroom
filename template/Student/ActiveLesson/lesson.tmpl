<script type="text/javascript">
    var requestUrl = "{$baseUrl}/command/{$teacher->id}/{$lesson->id}";
    {literal}
    ActiveLesson(false, {requestUrl: requestUrl});
    {/literal}
</script>

<h3>Lesson with teacher {$teacher->name}</h3>

<h4>{$lesson->caption}</h4>

<h4>{$lesson->subject}</h4>

<div class="htmlNicEditContainer whiteBoardContainer">
    <div id="lessonNicEditPanelContainer">
        <textarea id="lessonNicEditPanel">{$lesson->content}</textarea>
    </div>
</div>
